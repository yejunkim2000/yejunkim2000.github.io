---
layout: post
title: "[BOJ 28703] Double It"
date: 2026-05-16
category: 개발
author: yejunkim2000
tags: [백준, BOJ, C++, PS]
---


> **난이도:** Gold III | **태그:** 자료 구조, 그리디 알고리즘, 우선순위 큐
> **문제 링크:** https://www.acmicpc.net/problem/28703

---

## 문제

양의 정수로 이루어진 길이가 $N$인 배열 $A_1, \cdots, A_N$이 주어집니다. 당신은 원하는 만큼 다음 조작을 할 수 있습니다.


	배열에서 원하는 수 하나를 골라서 $2$를 곱합니다.


조작 이후 $A_1, \cdots, A_N$의 최댓값과 최솟값의 차이로 가능한 최솟값을 구하세요.

## 입력

첫 줄에 배열의 길이 $N$이 주어집니다. $(1 \le N \le 200\,000)$

둘째 줄에 $N$개의 양의 정수 $A_1, A_2, \cdots, A_N$이 주어집니다. $(1 \le A_i \le 10^9)$

## 출력

조작 이후 $A_1, \cdots, A_N$의 최댓값과 최솟값의 차이로 가능한 최솟값을 구하세요.

## 예제

### 예제 입력 1
```
6
31 41 51 92 65 3
```

### 예제 출력 1
```
40
```

---

## 풀이

```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int n, m = 0;
  cin >> n;
  priority_queue<int> q;
  while (n--) 
  {
    int x;
    cin >> x;
    m = max(m, x);
    q.push(-x);
  }
  int mx = m;
  int ans = mx + q.top();
  while (-q.top() < m) 
  {
    int x = -q.top();
    q.pop();
    ans = min(ans, mx - x);
    mx = max(mx, 2 * x);
    q.push(-2 * x);
  }
  cout << min(mx + q.top(), ans);
  return 0;
}
```


---

## 해설

### 핵심 아이디어
배열의 모든 원소를 2배씩 키워 최솟값과 최댓값 차이를 최소화. 우선순위 큐 그리디.

### 풀이 과정
1. 최솟값을 min-heap에서 꺼내 2배.
2. 현재 최댓값 - 최솟값이 최소가 될 때 갱신.
3. K번 반복 후 최솟값.

### 시간복잡도
O(K log N)