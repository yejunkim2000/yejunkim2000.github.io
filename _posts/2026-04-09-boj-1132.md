---
layout: post
title: "[BOJ 1132] 합"
date: 2026-04-09
category: 개발
author: yejunkim2000
tags: [백준, BOJ, C++, PS]
---


> **난이도:** Gold III | **태그:** 그리디 알고리즘
> **문제 링크:** https://www.acmicpc.net/problem/1132

---

## 문제

N개의 수가 주어진다. 이 숫자는 모두 자연수이고, 알파벳 A부터 J가 자리수를 대신해서 쓰여 있다. 이 알파벳은 모두 한 자리를 의미한다. 그리고, 각 자리수는 정확하게 알파벳 하나이다. 0으로 시작하는 수는 없다. 이때, 가능한 수의 합 중 최댓값을 구해보자.

## 입력

첫째 줄에 N이 주어진다. N은 50보다 작거나 같은 자연수이다. 둘째 줄부터 N개의 줄에는 각 수가 주어진다. 수의 길이는 최대 12이다. 적어도 한 알파벳은 수의 가장 처음에 주어지지 않는다.

## 출력

첫째 줄에 합의 최댓값을 출력한다.

## 예제

### 예제 입력 1
```
2
ABC
BCA
```

### 예제 출력 1
```
1875
```

### 예제 입력 2
```
1
ABCDEFGHIJ
```

### 예제 출력 2
```
9876543210
```

### 예제 입력 3
```
2
ABCDEFGHIJ
J
```

### 예제 출력 3
```
9876543202
```

### 예제 입력 4
```
10
A
BB
CCC
DDDD
EEEEE
FFFFFF
GGGGGGG
HHHHHHHH
IIIIIIIII
AJJJJJJJJJ
```

### 예제 출력 4
```
9973936905
```

### 예제 입력 5
```
5
GHJIDDD
AHHCCCA
IIJCEJ
F
HDBIGFJAAJ
```

### 예제 출력 5
```
9888114550
```

---

## 풀이

```cpp
#include <bits/stdc++.h>
using namespace std;
long long a[10];
long long ans;
bool Na[10];	
 
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int N, i, j;
	cin >> N;
	for (i = 0; i < N; i++) {
		string input;
		cin >> input;
		Na[input[0] - 'A'] = 1;	
		long long k = 1;
		for (j = input.size() - 1; j >= 0; j--) {
			a[input[j] - 'A'] += k;
			k *= 10;
		}
	}
	bool flg = 0;
	for (i = 0; i < 10; i++)
		if (!a[i])
			flg = 1;
	if (!flg) {
		long long min = 100000000000000000;
		int idx;
		for (i = 0; i < 10; i++) {
			if (!Na[i] && a[i] < min) {
				min = a[i];
				idx = i;
			}
		}
		a[idx] = 0;	
	}
	sort(a, a + 10, greater<>());	
	for (i = 0, j = 9; i < 10; i++, j--)
		ans += a[i] * j;
	cout << ans;
	return 0;
}
```


---

## 해설

### 핵심 아이디어
각 알파벳에 자릿수별 계수를 계산하여 큰 계수에 큰 숫자를 배정하는 그리디.

### 풀이 과정
1. 각 알파벳의 자릿수 가중치 합산.
2. 맨 앞자리에 등장한 알파벳은 0이 될 수 없음(Na 배열).
3. 가중치 내림차순으로 9,8,7... 배정. 단, 0 배정 가능한 가중치가 없으면 가장 작은 가중치를 0으로.

### 시간복잡도
O(N × 문자열 길이)